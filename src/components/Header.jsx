import React from "react";

const Header = () => (
  <div role="heading" aria-level="1">
    <header>
      <nav>
        <span>Bluestacks</span>
      </nav>
    </header>
  </div>
);

export default Header;
