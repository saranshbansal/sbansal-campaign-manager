import React from "react";

const Header = () => (
  <div role="heading" aria-level="1">
    <header>
      <nav>
        <span>Campaign Manager</span>
      </nav>
    </header>
  </div>
);

export default Header;
